---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: Cluster Profile
    path: architecture/%{architecture}/cluster/%{profile}.yaml
  - name: Replicas
    path: architecture/%{architecture}/replica/%{replica}.yaml
  - name: Architectures
    path: architecture/%{architecture}.yaml
  - name: Common
    path: common.yaml